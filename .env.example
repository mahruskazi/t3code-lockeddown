# This build is locked down: it has no cloud, telemetry, or update
# integrations, so there is no public configuration to inject at build time.
#
# Everything the app needs is local. This file is kept only so tooling that
# expects a repository-root `.env` keeps working.
